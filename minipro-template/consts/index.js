const COOKIE_KEY = 'cookie';

const TOKEN_KEY = 'token';

const baseUrl = 'https://service.yundui8.com/chengdu-play-test';

const baseUrlTest = 'localhost:8888/chengdu-play/user/login/';


module.exports = {
  COOKIE_KEY,
  TOKEN_KEY,
  baseUrl,
  baseUrlTest
}
